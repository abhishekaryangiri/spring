package mpf.dao;

import mpf.entity.User;

public interface UserDao {
    User findUserByMobileAndPassword(String mobileNumber, String password);
    User findUserByMobileAndTeacher(String mobileNumber, String favoriteTeacher);
    void updateUser(User user);
}
